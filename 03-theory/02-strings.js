const name = "Muhammadjon";
const age = 14;

function getAge() {
  return age;
}

// const old = 'Hello my name is ' + name + ' and my age is ' + age;

const output = `Hello my name is ${name} and my age is ${getAge()}`;

console.log(output);
